{
  "name": "example",
  "scripts": {
    "build": "vercel build",
    "deploy": "vercel build && vercel deploy --prebuilt"
  },
  "dependencies": {
    "@srvr-rndr/react": "0.0.1",
    "react": "experimental"
  },
  "devDependencies": {
    "vercel": "31.0.1"
  }
}
