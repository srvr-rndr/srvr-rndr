{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["./npm/", "./vercel/"],
      "inputs": ["./src/"]
    },
    "test": {},
    "lint": {},
    "format": {},
    "format:check": {}
  }
}
